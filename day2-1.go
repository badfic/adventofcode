package main

import (
    "fmt"
	"strconv"
	"strings"
)

func main() {
    var input string
    fmt.Scanln(&input)
	
	var checksum int
	
	fields := strings.Fields(input)

	for _, field := range(fields) {
		
	}

    fmt.Println(checksum)
}